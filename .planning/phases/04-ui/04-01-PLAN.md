---
phase: 04-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/test/setup.ts
  - src/components/__tests__/InteractionContainer.test.tsx
  - src/components/InteractionContainer.tsx
  - src/App.tsx
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
must_haves:
  truths:
    - "等待交互触发时，页面不发生任何有关弹窗或 Ticks 的 Re-render，重绘次数为 0"
    - "视频播放到交互判定点时，前台视频播放自动暂停"
    - "展示具有毛玻璃、渐变边框和高亮发光的选项弹窗卡片，卡片顶部拥有 10 秒渐变倒计时条"
    - "鼠标悬停于选项按钮时，执行微缩放动画，右侧箭头平滑向右移出"
    - "点击选项时，该选项高亮加载，其他选项置灰，无法进行二次重复点击"
    - "选项点击或倒计时结束 200ms 后，弹窗面板平滑缩放淡出，并分发跳转路由指令给状态机"
    - "10 秒倒计时结束用户未做选择时，系统安全超时自动流转至 defaultNextNodeId 或首个选项兜底"
  artifacts:
    - path: "src/components/InteractionContainer.tsx"
      provides: "解耦的高性能赛博毛玻璃交互选项弹窗组件"
    - path: "src/App.tsx"
      provides: "重构清理冗余状态后的骨架装总组件"
    - path: "src/components/__tests__/InteractionContainer.test.tsx"
      provides: "针对交互容器的核心测试套件，单测覆盖率 100%"
  key_links:
    - from: "src/components/InteractionContainer.tsx"
      to: "src/engine/NodeStateManager.ts"
      via: "直接监听事件订阅与调用 selectOption"
      pattern: "stateManager\\.on|stateManager\\.off|stateManager\\.selectOption"
    - from: "src/App.tsx"
      to: "src/components/InteractionContainer.tsx"
      via: "React 组件层级包裹与 stateManager 注入"
      pattern: "<InteractionContainer\\s+stateManager="
---

# Plan: 04-01 (顶层解耦交互 UI 与状态捕获)

<objective>
引入解耦的 React 交互容器 `InteractionContainer` 并重构 `App.tsx` 以完全剥离交互弹窗状态逻辑；使用 Vibrant Cyber-Glassmorphism 视觉美学、10 秒 GPU 硬件加速倒计时与强鲁棒超时安全自动流转机制，构建兼具流畅与高级感的分支交互卡窗；补齐测试基建，在 100% 覆盖率 of 自动化单测保障下完成本阶段。

Purpose: 隔离 Tick 渲染穿透以实现 60 FPS 极致流畅度，并建立高水准的 UI 动效和稳健的超时安全机制。
Output: InteractionContainer.tsx, InteractionContainer.test.tsx, 重构后的 App.tsx。
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui/04-CONTEXT.md
@.planning/phases/04-ui/04-RESEARCH.md
@.planning/phases/04-ui/04-UI-SPEC.md
@.planning/phases/04-ui/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 04-01-00: 安装测试依赖并配置 Vitest & 编写测试桩代码</name>
  <files>package.json, vite.config.ts, src/test/setup.ts, src/components/__tests__/InteractionContainer.test.tsx</files>
  <action>
    1. 在 package.json 中安装测试开发依赖包：vitest、jsdom、@testing-library/react、@testing-library/jest-dom、@vitejs/plugin-react（如果不存在的话）。
    2. 在 vite.config.ts 中引入并配置 test 字段（声明 environment: 'jsdom', setupFiles: './src/test/setup.ts'）。
    3. 创建 src/test/setup.ts，导入 @testing-library/jest-dom 以扩展 Jest 匹配器。
    4. 在 src/components/__tests__/InteractionContainer.test.tsx 中编写测试桩代码（包含针对 UI-01 至 UI-05 需求的空测试用例或 Mock 结构）。
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/InteractionContainer.test.tsx</automated>
  </verify>
  <done>测试依赖安装完成，Vitest 配置通过自检，桩测试代码能够被测试运行器顺利解析和执行。</done>
</task>

<task type="auto">
  <name>Task 04-01-01: 实现解耦的 InteractionContainer 容器与低频订阅模型 (UI-01, UI-02)</name>
  <files>src/components/InteractionContainer.tsx, src/App.tsx, src/components/__tests__/InteractionContainer.test.tsx</files>
  <action>
    1. 创建 src/components/InteractionContainer.tsx，传入 stateManager，通过 React.useEffect 在挂载时订阅 'interactionTriggered' 和 'nodeChanged' 事件，且必须在清理函数中正确调用 off() 以完美注销订阅（防御 React StrictMode 双击挂载）。
    2. 组件在监听到 interactionTriggered 事件时显示交互卡片，在监听到 nodeChanged 时隐藏交互卡片。
    3. 重构 src/App.tsx，彻底清除 activeInteraction 相关的 React state 与直接内联渲染逻辑，将其替换为渲染 `<InteractionContainer stateManager={stateManager} />`。确保 App.tsx 保持绝对静态，完全不引入交互弹窗控制 state，防范 Tick 渲染穿透。
    4. 在 InteractionContainer.test.tsx 中编写针对局部订阅发布、清理注销订阅以及 Tick 不穿透 App 的单元测试。
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/InteractionContainer.test.tsx</automated>
  </verify>
  <done>InteractionContainer 自主管理其显隐状态，App.tsx 冗余 state 完全剥离，测试套件中关于事件解耦订阅的用例全部通过。</done>
</task>

<task type="auto">
  <name>Task 04-01-02: 实现 10s GPU 级零重绘倒计时条与超时安全自动流转机制 (UI-03, UI-04)</name>
  <files>src/components/InteractionContainer.tsx, src/components/__tests__/InteractionContainer.test.tsx</files>
  <action>
    1. 在 InteractionContainer.tsx 中引入 `progressWidth` 状态（'100%' 或 '0%'）。在交互触发时，利用下一帧 setTimeout(..., 50) 将其改为 '0%'，配合 CSS 的 `transition: width 10s linear` 硬件加速，全程 0 render 实现倒计时缩短。
    2. 实现 10s 超时定时器（autoTransitionTimer）：若 10s 内未做选择，自动触发 stateManager.selectOption 跳转到当前节点的 defaultNextNodeId，若无则以第一个选项 targetNodeId 进行安全兜底跳转。
    3. 实现 isLocked 原子防重复锁定状态：在用户手动点击选项或超时跳转触发的瞬间，立即设置 isLocked 为 true，禁用所有交互，并给被点击选项显示 Loading 态（'⚡ Loading Branch...'），给其他选项置灰，在 200ms 延迟动画后分发跳转指令。
    4. 在 InteractionContainer.test.tsx 中编写倒计时挂载、自动超时流转兜底跳转、多点击并发拦截锁的单元测试用例。
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/InteractionContainer.test.tsx</automated>
  </verify>
  <done>10 秒倒计时条缩短与超时跳转逻辑无缝闭环，isLocked 单向锁定成功拦截多重点击，对应测试用例 100% 通过。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-01-03: 丰富 Vibrant Cyber-Glassmorphism 视觉美学与微动效并进行全链路 UAT 校验 (UI-05)</name>
  <files>src/components/InteractionContainer.tsx</files>
  <action>
    1. 运用 Tailwind CSS v4 样式类修饰面板与按钮，实现磨砂玻璃模糊（backdrop-blur-xl bg-slate-900/60 border border-white/10 shadow-[0_20px_50px_rgba(0,0,0,0.6)]）及发光渐变效果。
    2. 实现选项悬停的 scale-[1.02] 微缩放动效、右侧箭头滑出动画、以及锁定退出时的 200ms 平滑缩放淡出（scale-95 opacity-0 duration-200）。
    3. 运行完整测试并通过编译打包校验。
  </action>
  <verify>
    1. 运行 `npm run build` 确保 TypeScript 编译和 Vite 打包 100% 成功。
    2. 运行 `npm run dev` 启动开发服务器。打开浏览器访问，观察视频在 10 秒到达时是否能暂停并呈现精致的毛玻璃弹窗。
    3. 鼠标悬停于选项按钮时验证是否流畅执行 `scale-[1.02]` 缩放和右侧箭头平滑移出动画；点击后观察选项是否被瞬间置灰锁定，并在 200ms 内平滑淡出，随后底层双实例进行瞬间对调切换。
  </verify>
  <done>赛博毛玻璃视觉效果极佳，3D Hover 动效丝滑平畅，UAT 全链路手势解锁、超时及瞬间硬切转场完全符合美学和解耦期望。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Click → State Machine | 选项按钮被高频点击或在临界超时的一毫秒并发点击，不受信输入越过此边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | InteractionContainer.tsx | mitigate | 使用 isLocked 原子状态，点击或超时触发后瞬间拦截一切后续事件分发，彻底屏蔽并发状态抖动 (D-08) |
| T-04-SC | Tampering | npm installs | mitigate | 安装测试依赖包时均使用 npm 官方锁定的高安全版本包，防止依赖投毒 |
</threat_model>

<verification>
运行全体单元测试命令：
npx vitest run

确保生产环境构建通过：
npm run build
</verification>

<success_criteria>
- 所有 3 个核心单元测试套件测试全部通过 (Pass)
- App.tsx 冗余 UI state 彻底清除，InteractionContainer 独立局部低频订阅
- 赛博炫彩毛玻璃弹窗卡片展现卓越，具备 10s GPU 零重绘倒计时和超时自动跳转
- 点击锁定及 200ms 缩放淡出完美平畅，与底层瞬间硬切实现天衣无缝的拼合
- Vite 打包和生产构建完全成功
</success_criteria>

<output>
Create `.planning/phases/04-ui/04-01-SUMMARY.md` when done
</output>
