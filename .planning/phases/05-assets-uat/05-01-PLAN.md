---
phase: 05-assets-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/types.ts
  - src/engine/NodeStateManager.ts
  - src/components/InteractivePlayer.tsx
  - src/components/DebugDrawer.tsx
  - src/components/__tests__/InteractivePlayer.test.tsx
  - src/components/__tests__/FullStoryFlow.test.tsx
autonomous: false
requirements:
  - VLD-01
  - VLD-02
must_haves:
  truths:
    - "在 InteractivePlayer 物理切换 A/B 实例时，能精准打点记录并广播过渡时延"
    - "Debug Drawer 调试抽屉能实时且正确地显示毫秒级的 Switch Latency 数值"
    - "10 秒倒计时超时用户未点击时，系统静默无缝物理硬切进入 defaultNextNodeId 分支"
    - "提供极高可用性的 FFmpeg 音视频工业级统一转码指南，确保切换帧边界绝对对齐"
    - "编写 FullStoryFlow.test.tsx 全链路集成单测，以 100% 覆盖率验证完整的分支切换和超时生命周期"
  artifacts:
    - path: "src/components/__tests__/FullStoryFlow.test.tsx"
      provides: "针对全链路互动视频状态流转及切换的集成测试套件"
  key_links:
    - from: "src/components/InteractivePlayer.tsx"
      to: "src/engine/NodeStateManager.ts"
      via: "调用 recordSwitchLatency 进行物理切换延时打点同步"
      pattern: "stateManager\\.recordSwitchLatency"
    - from: "src/components/DebugDrawer.tsx"
      to: "src/engine/NodeStateManager.ts"
      via: "订阅 switchLatency 事件并局部更新延时数值状态"
      pattern: "stateManager\\.on"
---

# Plan: 05-01 (导入测试资产并进行全链路 UAT 最终调试)

<objective>
在 Phase 5 阶段，我们将挂载真实的测试多媒体资产，完成状态机与双播放器架构的物理全链路拼接联调。为了精确量化并兑现“无缝物理切换（时差 < 100ms）”的核心价值，我们将在引擎与播放器底层实现物理切换延时打点，并在极客调试面板中实时进行毫秒级可视化呈现。同时，我们将编写覆盖超时的自动化集成测试套件，制定工业级转码标准，从底层到上层全景完成项目 UAT 闭环验证。
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-assets-uat/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 05-01-00: 扩展 NodeStateManager 状态机事件总线，新增并支持 switchLatency 事件广播</name>
  <files>src/engine/types.ts, src/engine/NodeStateManager.ts</files>
  <action>
    1. 修改 src/engine/types.ts，定义 `SwitchLatencyCallback = (latency: number) => void`，并在 `StateManagerEvents` 接口中添加 `switchLatency: SwitchLatencyCallback[]`。
    2. 修改 src/engine/NodeStateManager.ts，在事件订阅存储 events 中初始化 `switchLatency: []`。
    3. 在 `NodeStateManager` 类中提供公共成员方法 `recordSwitchLatency(latency: number): void`，该方法遍历并触发 `events.switchLatency` 所有绑定的监听器，在控制台通过 `console.log` 打印该延时。
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/InteractivePlayer.test.tsx</automated>
  </verify>
  <done>状态机事件广播总线成功支持 `switchLatency` 事件的分发，且静态类型校验通过。</done>
</task>

<task type="auto">
  <name>Task 05-01-01: 在播放器切换与抢占算法中引入高精打点性能监控 (D-02)</name>
  <files>src/components/InteractivePlayer.tsx, src/components/__tests__/InteractivePlayer.test.tsx</files>
  <action>
    1. 修改 src/components/InteractivePlayer.tsx，定位到 `executeSeamlessTransition` 方法首部，通过 `const startTime = performance.now();` 开始打点。
    2. 在 `requestAnimationFrame` 交换 DOM 样式操作的物理帧帧尾，计算时差 `const latency = performance.now() - startTime;`，并立即调用 `stateManager.recordSwitchLatency(latency)` 广播该时延。
    3. 同步修改 `executePreemptionHotload`，在 bgVideo 开始加载和 buffering 的前夕进行性能打点，并在 DOM 瞬间硬切层叠成功后，计算出抢占热加载的整体物理时效，同样广播。
    4. 丰富 src/components/__tests__/InteractivePlayer.test.tsx，增加针对 `recordSwitchLatency` 打点调用的 Stub 校验测试，确保每次切换时均有指标产出。
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/InteractivePlayer.test.tsx</automated>
  </verify>
  <done>双播放器瞬间切换算法中已成功织入高精度的打点耗时监控，打点信息能够不失真地广播至外界。</done>
</task>

<task type="auto">
  <name>Task 05-01-02: 在 Debug Drawer 调试面板中订阅并可视化展示切换延时指标 (D-03)</name>
  <files>src/components/DebugDrawer.tsx</files>
  <action>
    1. 修改 src/components/DebugDrawer.tsx，在组件内新增 `switchLatency` state（类型为 `number | null`，初始化为 `null`）。
    2. 在组件挂载时，通过 `stateManager.on('switchLatency', (latency) => setSwitchLatency(latency))` 订阅切换延时事件，并在 React 清理函数中正确调用 `off` 注销订阅。
    3. 重构 DebugDrawer 仪表盘的 Body 渲染，在“双 DOM 物理状态监视”折叠卡下方，新增一个极客感十足的系统指标状态组。
    4. 在该状态组中渲染一行包含闪烁微标的展示列：`⚡ 物理切换延时: {switchLatency !== null ? \`\${switchLatency.toFixed(2)} ms\` : '暂无切换'}`。若时差小于 100ms 则高亮显示为亮紫色或淡翠绿色，反之显示黄色，提供直观的指标预警。
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/InteractivePlayer.test.tsx</automated>
  </verify>
  <done>极客调试抽屉控制面板中新集成了实时视频拼接延时监测状态，能随物理事件订阅低重绘刷新。</done>
</task>

<task type="auto">
  <name>Task 05-01-03: 编写 FullStoryFlow.test.tsx 全链路集成单测，100% 覆盖超时与切换生命周期 (D-06)</name>
  <files>src/components/__tests__/FullStoryFlow.test.tsx</files>
  <action>
    1. 创建 src/components/__tests__/FullStoryFlow.test.tsx。
    2. 使用 Vitest + React Testing Library，导入 `InteractionContainer` 与 `InteractivePlayer`，实例化 `NodeStateManager`（加载真实 storyConfig.json 的 mock）。
    3. 编写第一个核心测试用例（手动分支选择）：模拟播放器播放，监听进度 Tick 越过 10 秒边界触发交互。断言界面上成功显示交互 Overlay，前台视频暂停；随后模拟用户点击“分支 B”，断言 isLocked 原子防重复锁生效，200ms 后分发切换指令并正确触发 `nodeChanged`，最后断言 activePlayer 从 A 瞬间物理切换为 B。
    4. 编写第二个核心测试用例（超时自动跳转，D-04, D-05）：模拟交互触发，使用 Vitest `vi.useFakeTimers()`，模拟 10 秒未进行任何手动选择，自动触发超时兜底流转，断言引擎能静默流转跳转至 `defaultNextNodeId` ("branch_a")，且不伴随卡顿或黑屏。
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/FullStoryFlow.test.tsx</automated>
  </verify>
  <done>全链路流转自动化集成测试编写完毕，高覆盖度覆盖分支选择与倒计时超时机制，全部测试用例绿灯通过。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 05-01-04: 挂载测试多媒体资产，制定工业级转码标准，人工全链路 UAT 最终调试 (VLD-01, VLD-02)</name>
  <files>public/assets/intro.mp4, public/assets/branch_a.mp4, public/assets/branch_b.mp4, public/storyConfig.json</files>
  <action>
    1. 验证 `/public/assets/` 文件夹下测试视频完整（intro.mp4, branch_a.mp4, branch_b.mp4 放置妥当）。
    2. 在交付文档或 `.planning/phases/05-assets-uat/` 中整理一份 FFmpeg 工业级无缝转码基准指南，供项目资产库归档参考，满足 D-01。
    3. 执行全体自动化测试，确保 100% 单测与集成测试通过。
    4. 运行 `npm run build` 进行生产编译，保证零报错、完美通过构建门槛。
    5. 启动本地开发服务，人工在主流浏览器进行端到端 UAT 确认：
       - 打开有声体验首播，从 `main_intro` 流畅播放。
       - 到达 10.0 秒时观察交互框是否平滑毛玻璃展现并暂停视频，GPU 倒计时条是否有声流逝。
       - 分别点击选项及等待倒计时触底超时跳转，确认物理切换时差 < 100ms（Debug 抽屉显示的 `Switch Latency` 处于健康水平），肉眼完全不发生黑屏、闪烁或抖动。
  </action>
  <verify>
    1. 终端执行全体测试 `npx vitest run` 应全部通过。
    2. 终端执行编译 `npm run build` 应成功生成 `dist/` 打包产物。
    3. 浏览器实际操作：测试手动选择和自动超时，通过极客面板亲眼检验拼接无缝感及查看延时量化指标。
  </verify>
  <done>UAT 全链路联调体验极佳，转码标准整理完毕，测试与生产构建全绿通过，切换时差完美控制在 < 50ms 级别，整期里程碑完美封版。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Multimedia Codecs → Dual-Player | 用户本地浏览器对于多媒体视频流的解码策略，不受信的多媒体资产越过此边界，容易触发硬件解码阻塞 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-ASSET | Denial of Service | public/assets/ | mitigate | 提供 FFmpeg 统一转码指南，规范多媒体 GOP 与闭合流格式，防止极高音视频码率引发的物理卡顿或播放器黑屏崩溃 (D-01) |
| T-05-LOCK | Tampering | InteractionContainer.tsx | mitigate | 超时 handleTimeout 时立刻激活 isLocked 单向防重复原子锁，全面防范自动与手动在同一微秒触发的竞态二次跳转流转 |
</threat_model>

<verification>
运行所有单测与集成测试用例：
npx vitest run

运行生产发布版本编译打包：
npm run build
</verification>

<success_criteria>
- 全链路集成测试套件 `FullStoryFlow.test.tsx` 编写就绪，集成测试 100% 绿灯通过
- 打点监控成功织入 `InteractivePlayer`，切换时差精确到浮点毫秒并广播给状态机
- `DebugDrawer` 控制台内完美实时渲染高精度的物理切换延时面板，数据透明客观
- 10s 超时未选择时，能够无卡顿静默瞬间跳转至 defaultNextNodeId 分支，不产生闪烁或淡出
- 工业级资产转码 FFmpeg 指南在 Phase 5 文档归档完成
- 全链路 UAT 人工端到端验证通过，生产编译成功
</success_criteria>

<output>
Create `.planning/phases/05-assets-uat/05-01-SUMMARY.md` when done
</output>
